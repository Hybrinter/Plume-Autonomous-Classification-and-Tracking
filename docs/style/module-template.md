# <dotted.module.path>

**Source:** `packages/<pkg>/src/<...>/<module>.py`
**Kind:** module | pure module | app shell | driver | stub

## Purpose

One to three sentences. State what the module does in the present tense.

## Public interface

| Name | Kind | Description |
| --- | --- | --- |
| `Symbol` | class \| function \| constant | Short fact |

## Inputs and outputs

State the inputs and outputs of each public entry point.

## Behavior

1. First step.
2. Second step.

## Errors and faults

List each `Err` value or `FaultCode` the module can produce, and the trigger.
Write `None.` if the module raises only at process startup or has no faults.

## Messages

List published and subscribed message types. Write `None.` for pure modules.

## Configuration

List config fields the module reads. Write `None.` if none.

## Constraints

State invariants as facts. Mark stubs here when the unit is a placeholder.

## Related documents

- Parent and sibling descriptive pages only.
