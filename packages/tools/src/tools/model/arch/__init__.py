"""Segmentor and classifier network builders.

Importing this package does not import torch. Import `tools.model.arch.unet` or
`tools.model.arch.classifier` only from train/export after the train extra is
installed.
"""
