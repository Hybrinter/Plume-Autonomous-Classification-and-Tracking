# <dotted.package.path>

**Source:** `packages/<pkg>/src/<...>/<dir>/`
**Kind:** package | subsystem app | driver set | stub

## Purpose

One to three sentences. State what this directory group does.

## Contents

| Item | Type | Description |
| --- | --- | --- |
| [`child`](dir/child.md) | module \| package | Short fact |

## Package interface

State what the package `__init__.py` re-exports. Write `None.` if empty.

## Interactions

State which messages enter and leave. State which HAL protocols the code uses.
Write `None.` if the group is pure library code with no bus use.

## Constraints

State group-level invariants as facts.

## Related documents

- Parent and sibling descriptive pages only.
