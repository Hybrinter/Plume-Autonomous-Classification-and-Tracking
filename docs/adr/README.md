# Repository ADR files

Decision bodies in this directory are frozen repository-scope records.
Use [`../adr.md`](../adr.md) as the human-readable index (`ADR-REPO-NNNN`).

Do not rewrite accepted decision bodies. You may change only `Status` and
`Superseded-by` when a later record supersedes one.
